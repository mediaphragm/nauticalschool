<h1>Result list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Score</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('result_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.score }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('result_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('result_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('result_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
